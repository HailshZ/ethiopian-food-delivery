<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="glass-card auth-card">
        <div class="text-center mb-4">
          <div class="auth-icon"><i class="bi bi-person-plus-fill"></i></div>
          <h3 class="fw-bold">
            <%= t('register') %>
          </h3>
          <p class="text-muted">
            <%= t('join_us') %>
          </p>
        </div>

        <% if (typeof errors !=='undefined' && errors && errors.length> 0) { %>
          <% errors.forEach(err=> { %>
            <div class="alert alert-danger rounded-3">
              <%= err.msg %>
            </div>
            <% }) %>
              <% } %>

                <form action="/register" method="post">
                  <div class="row g-3">
                    <div class="col-md-6">
                      <label class="form-label fw-semibold"><i class="bi bi-person me-1"></i>
                        <%= t('full_name') %>
                      </label>
                      <input type="text" name="name" class="form-control rounded-3 py-2"
                        placeholder="<%= t('name_placeholder') %>"
                        value="<%= typeof name !== 'undefined' ? name : '' %>" required>
                    </div>
                    <div class="col-md-6">
                      <label class="form-label fw-semibold"><i class="bi bi-phone me-1"></i>
                        <%= t('phone') %>
                      </label>
                      <input type="tel" name="phone" class="form-control rounded-3 py-2"
                        placeholder="<%= t('phone_placeholder') %>"
                        value="<%= typeof phone !== 'undefined' ? phone : '' %>">
                    </div>
                    <div class="col-12">
                      <label class="form-label fw-semibold"><i class="bi bi-envelope me-1"></i>
                        <%= t('email') %>
                      </label>
                      <input type="email" name="email" class="form-control rounded-3 py-2"
                        placeholder="<%= t('email_placeholder') %>"
                        value="<%= typeof email !== 'undefined' ? email : '' %>" required>
                    </div>
                    <div class="col-md-6">
                      <label class="form-label fw-semibold"><i class="bi bi-lock me-1"></i>
                        <%= t('password') %>
                      </label>
                      <input type="password" name="password" class="form-control rounded-3 py-2"
                        placeholder="<%= t('password_placeholder') %>" required>
                    </div>
                    <div class="col-md-6">
                      <label class="form-label fw-semibold"><i class="bi bi-lock-fill me-1"></i>
                        <%= t('confirm_password') %>
                      </label>
                      <input type="password" name="password2" class="form-control rounded-3 py-2"
                        placeholder="<%= t('confirm_password_placeholder') %>" required>
                    </div>
                  </div>
                  <button type="submit" class="btn btn-primary w-100 rounded-pill py-2 fw-bold mt-4 mb-3">
                    <i class="bi bi-person-plus me-2"></i>
                    <%= t('register') %>
                  </button>
                  <p class="text-center text-muted mb-0">
                    <%= t('already_have_account') %> <a href="/login" class="text-primary fw-semibold">
                        <%= t('login') %>
                      </a>
                  </p>
                </form>
      </div>
    </div>
  </div>
</div>